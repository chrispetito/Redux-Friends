// import { } from "../actions";

const initialState = {
  friends: [],
};
export const friendsReducer = (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
